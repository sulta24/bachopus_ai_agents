# AI Orchestrator

Интеллектуальная система оркестрации для анализа данных и взаимодействия с внешними API.

## 🚀 Возможности

- **Многоэтапная обработка запросов** - планирование, выполнение, обратная связь
- **Интеграция с Datadog** - получение метрик и временных рядов
- **Умное форматирование контекста** - адаптивная обработка данных
- **Bearer Token аутентификация** - безопасное взаимодействие с API
- **RESTful API** - простое интегрирование с внешними системами

## ⚡ Быстрый старт

### Установка
```bash
# Клонировать репозиторий
git clone <repository-url>
cd AI_Orchestrator

# Создать виртуальное окружение
python -m venv venv
venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt
```

### Конфигурация
```bash
# Скопировать пример конфигурации
cp .env.example .env

# Настроить переменные окружения
# OPENAI_API_KEY=your_openai_key
# DATADOG_API_KEY=your_datadog_key
# DATADOG_APP_KEY=your_datadog_app_key
```

### Запуск
```bash
uvicorn main:app --reload --port 8000
```

## 📡 API

### POST /orchestrate
Основной эндпоинт для обработки запросов.

**Заголовки:**
```
Authorization: Bearer <token>
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "service_id": "test-service",
  "session_id": "test-session", 
  "user_message": "Analyze system performance"
}
```

**Ответ:**
```json
{
  "answer": "Анализ выполнен...",
  "session_id": "test-session",
  "timestamp": "2024-01-19T10:30:00Z"
}
```

## 📁 Структура проекта

```
├── agents/          # Специализированные агенты
├── core/           # Основная логика системы
├── main.py         # FastAPI приложение
├── requirements.txt # Зависимости
└── trae-logs.md    # Логи разработки
```

## 🔧 Основные компоненты

- **AIOrchestrator** - центральный координатор обработки
- **BackendClient** - клиент для взаимодействия с API
- **DatadogClient** - интеграция с Datadog
- **ReasoningState** - управление состоянием рассуждений

## 📝 Документация API

После запуска доступна по адресу: `http://localhost:8000/docs`

## 🛠 Разработка

```bash
# Тестирование API ключей
python test_api_key.py

# Тестирование Datadog
python test_datadog.py
```